const BaseListener = require("./BaseListener");

class PaymentListener extends BaseListener {
  constructor() {
    super();
  }

  setupSubscriptions() {}
}

module.exports = PaymentListener;
